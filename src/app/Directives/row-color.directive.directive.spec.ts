import { RowColorDirectiveDirective } from './row-color.directive.directive';

describe('RowColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RowColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
