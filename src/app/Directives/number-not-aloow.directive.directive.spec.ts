import { NumberNotAloowDirectiveDirective } from './number-not-aloow.directive.directive';

describe('NumberNotAloowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberNotAloowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
