import { TrimePipePipe } from './trime.pipe.pipe';

describe('TrimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
