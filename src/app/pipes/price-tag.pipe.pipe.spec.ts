import { PriceTagPipePipe } from './price-tag.pipe.pipe';

describe('PriceTagPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceTagPipePipe();
    expect(pipe).toBeTruthy();
  });
});
